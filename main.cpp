#include <iostream>

int main() {
    int num;
    printf("inserisci un numero: ");
    scanf("%d", &num);
    if(num>0) {
        printf("il numero %d e' positivo\n", num);
    } else (num<0);{
        printf("il numero %d e' negativo\n", num);
    }
}
